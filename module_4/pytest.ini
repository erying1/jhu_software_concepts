[pytest]
addopts = -v --cov=src --cov-report=term-missing --cov-fail-under=98
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src
markers =
    web: Flask route/page tests
    buttons: "Pull Data" and "Update Analysis" behavior
    analysis: formatting/rounding of analysis output
    db: database schema/inserts/selects
    integration: end-to-end flows
minversion = 6.0
testpaths = tests
console_output_style = progress

# Add this line at the end of pytest.ini:
# Updated: 2025-02-15